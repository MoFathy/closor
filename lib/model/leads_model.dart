import 'package:flutter/cupertino.dart';

class LeadsModel {
  String name, product, sourceUrl, stateUrl;
  Color color;

  LeadsModel(
      {this.name, this.product, this.sourceUrl, this.stateUrl, this.color});
}
